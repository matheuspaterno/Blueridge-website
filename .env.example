############################################
# Example Environment Configuration
# Copy to .env.local and fill in real values. DO NOT COMMIT REAL SECRETS.
############################################

# Core
APP_BASE_URL=http://localhost:5173
DEFAULT_OWNER_ID=default_owner

# OpenAI
OPENAI_API_KEY=sk-your-key

# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# SMTP / Email (choose one provider: PrivateEmail, SendGrid, etc.)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
SMTP_FROM="Your Brand <no-reply@example.com>"
BOOKINGS_FROM_EMAIL=bookings@example.com
FROM_EMAIL=bookings@example.com
FROM_NAME="Your Brand"
# DEBUG_BOOKING=true
# SMTP_DEBUG=true

# CalDAV (if using CalDAV scheduling)
CALDAV_BASE_URL=https://mail.privateemail.com/dav
CALDAV_USERNAME=services@your-domain.com
CALDAV_APP_PASSWORD=your-caldav-app-password
PRIMARY_TIMEZONE=America/New_York

# Google OAuth (optional if using Google Calendar)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:5173/api/oauth/google/callback

# Stripe (optional)
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
STRIPE_PRICE_GROWTH=price_xxx
STRIPE_PRICE_STARTER=price_xxx

# Feature flags
AI_MODEL_ONLY=false